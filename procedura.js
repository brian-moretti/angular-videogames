/*
* Installare NgMaterial -> ng add @angular/material
* Installare Angular Gauge per Rating
* Importare Moduli in app.module
! Setup ultimato

* Creare componente 'search-bar' e inserirlo in app.component Template
* Impostare stile componente
* Definire event-binding ngSubmit nel <form>
* Nel componente importare ngForm, Router come dipendenza e creare metodo Submit per navigare

* Creare componente Home come default per l'applicazione
* Settare il routing per cui 'home' diventa il componente di defaut e anche quello attivo quando si esegue una ricerca
* Creare il Template con funzione di filtro parametro e lo stile

* Creare servizio HTTP e iniettare HttpClient
* Creare metodo per fetch, attraverso API url, i games. Il metodo ritorna un Observable attraverso una chiamata http.get()
* Definire le interfacce per l'Observable che ritorna

* Creazione Http Interceptiors e definirli con il decoratore @injectable, come servizi
* Utilizzare metodo intercept che ritorna un Observable
* Inserire gli interceptors in app.module nell'array provide
* Definire in 'home' i metodi per richiamare i dati dal http service e compilare il Template con le direttive e le proprietà

* Creare metodo per filtrare i games in base al Select
* Rimuovere le subscribe alla distruzione del componente

*/



/*
! TUTORIAL SEMI-FUNZIONANTE:
* API MOVIES & SCREEN NON ACCESSIBILI

*/
